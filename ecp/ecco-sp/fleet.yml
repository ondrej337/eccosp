namespace: eccosp

# Custom helm options
helm:
  # The release name to use. If empty a generated release name will be used
  releases:
    - name: ecp
      chart: ./ecp-endpoint
      valuesFiles:
        - values-ecp.yaml
    - name: artemis
      chart: ./eccosp-artemis
      valuesFiles:
        - values-artemis.yaml
  